<% unless flash.empty? %>
  <script type="text/javascript">
    <% flash.each do |f| %>
      <% type = f[0].to_s.gsub('alert','error').gsub('notice', 'info') %>
      toastr['<%= type %>']('<%= f[1] %>')
    <% end %>
  </script>
<% end %>

<!-- tis is the example is gave to explain what is happening above the left part of the equals is the type and the second is the content of the message or f[1]
 flash[:notice] = "email is incorrect" -->
